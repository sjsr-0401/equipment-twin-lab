# 2026-07-01 작업 로그

## 오늘 한 일

- PR #12를 병합한 뒤 `main`을 최신화했다.
- `goal/015-motion-scenario-actions` 브랜치에서 Goal 015를 진행했다.
- `MotionAxis`를 Scenario JSON action으로 실행할 수 있게 했다.
- CLI batch Markdown 리포트에 `Motion Axes` 컬럼을 추가했다.
- 정상 모션 시나리오와 모션 Timeout 시나리오를 추가했다.
- 회귀 테스트에 모션 시나리오 2개를 추가했다.
- Draft PR #13을 생성했다.
- GitHub Actions push/pull_request CI 성공을 확인했다.

## 왜 필요한가

Goal 014의 `MotionAxis`는 축 자체 모델이었다.

이번 작업은 그 축을 사용자가 고를 수 있는 시나리오 파일로 끌어올린 것이다.

즉, 아래처럼 발전했다.

```text
테스트 코드 안의 MotionAxis
    ↓
JSON으로 실행 가능한 MotionAxis
    ↓
나중에 Unity UI / Equipment Template / Product Recipe가 재사용 가능한 구조
```

## 바뀐 파일

- `src/EquipmentTwin.Core/Scenarios/ScenarioStepAction.cs`
- `src/EquipmentTwin.Core/Scenarios/ScenarioStep.cs`
- `src/EquipmentTwin.Core/Scenarios/ScenarioRunner.cs`
- `src/EquipmentTwin.Cli/Program.cs`
- `tests/EquipmentTwin.Core.Tests/Program.cs`
- `scenarios/motion-axis-normal.json`
- `scenarios/motion-axis-timeout.json`
- `goals/015-motion-scenario-actions.md`
- `README.md`
- `docs/architecture.md`
- `docs/core-validation.md`
- `state/loop-state.md`
- `state/triage.md`
- `state/comprehension.md`
- `plan.md`

## 검증 결과

- Release 빌드: 성공
- 경고: 0개
- 오류: 0개
- 로컬 테스트: 51개 통과
- CLI batch 실행: 9개 시나리오 통과
- Markdown 리포트에서 `Motion Axes` 컬럼 확인
- GitHub Actions push 이벤트 CI: 성공
- GitHub Actions pull_request 이벤트 CI: 성공

실행한 명령:

```powershell
dotnet build EquipmentTwinLab.sln --no-restore --configuration Release
dotnet run --project tests\EquipmentTwin.Core.Tests\EquipmentTwin.Core.Tests.csproj --no-restore --configuration Release
dotnet run --project src\EquipmentTwin.Cli\EquipmentTwin.Cli.csproj --no-restore --configuration Release -- batch scenarios --default-timeouts --report artifacts\scenario-report.md
```

## 막힌 점과 해결 방법

- 큰 기능 구현 막힘은 없었다.
- 문서 확인 중 PowerShell 출력에서 한글이 깨져 보였지만, `-Encoding UTF8`로 읽으면 정상이라 파일 자체 문제는 아니었다.
- `artifacts/scenario-report.md`는 검증 산출물이라 Git에 포함하지 않았다.

## 보류한 판단

- 축 알람을 장비 전체 `Alarmed` 상태와 연결하지 않았다.
- 모션 축은 아직 공정 상태머신의 자동 진행 조건으로 쓰지 않는다.
- 속도/가속도/감속도 프로파일은 아직 모델링하지 않는다.
- Unity 시각화는 아직 시작하지 않았다.

## 소프트웨어 아키텍처 설명

```text
Scenario JSON
    ↓
ScenarioStep
    ↓
ScenarioRunner
    ↓
MotionAxis
    ↓
Scenario expectation
    ↓
CLI Markdown report
```

`ScenarioStepAction`은 “JSON에서 사용할 수 있는 명령 이름 목록”이다.

`ScenarioStep`은 그 명령에 필요한 필드를 검증한다. 예를 들어 `StartMotionMove`는 `axis`, `targetPosition`, `durationMilliseconds`가 필요하다.

`ScenarioRunner`는 실제로 `MotionAxis`를 찾아서 명령을 실행한다. 축은 이름별로 보관되므로 `X`축에 Servo On을 한 뒤 다음 step에서 같은 `X`축을 Home/Move할 수 있다.

CLI는 계산하지 않는다. Core가 실행한 축 상태를 읽어서 리포트에 표시한다.

## 유지보수할 때 봐야 할 파일

- 모션 action 이름: `src/EquipmentTwin.Core/Scenarios/ScenarioStepAction.cs`
- JSON 필드와 validation: `src/EquipmentTwin.Core/Scenarios/ScenarioStep.cs`
- 모션 action 실행: `src/EquipmentTwin.Core/Scenarios/ScenarioRunner.cs`
- 리포트 표시: `src/EquipmentTwin.Cli/Program.cs`
- 시나리오 예시: `scenarios/motion-axis-normal.json`, `scenarios/motion-axis-timeout.json`
- 테스트: `tests/EquipmentTwin.Core.Tests/Program.cs`

## 사용자가 이해해야 할 개념

이번 작업의 핵심은 “코드로만 테스트하던 동작을 데이터로 실행하게 만든 것”이다.

장비 시뮬레이터가 커지면 사용자가 매번 C# 코드를 고쳐서 테스트할 수 없다. JSON 같은 데이터 파일로 장비 동작을 표현해야 Unity UI, CLI, 자동화 루프가 같은 시나리오를 돌릴 수 있다.

## 다음 작업

1. PR #13 병합 여부를 결정한다.
2. 병합 후 `main`을 최신화한다.
3. 다음 Goal 후보로 Equipment Template / Product Recipe를 시작한다.

---

## Goal 016: Equipment Template / Product Recipe

## 오늘 추가로 한 일

- PR #13을 Draft 해제하고 squash merge했다.
- 로컬 `main`을 최신화했다.
- `goal/016-equipment-template` 브랜치를 만들었다.
- Equipment Template / Product Recipe 최소 모델을 추가했다.
- `templates/vision-inspection-cell.json` 샘플 장비 템플릿을 추가했다.
- 장비 템플릿 테스트 5개를 추가했다.
- Draft PR #14를 생성했다.
- GitHub Actions push/pull_request CI 성공을 확인했다.

## 왜 필요한가

사용자가 원하는 최종 방향은 “장비, 제품, 공정, 검사 조건, 트러블 조건을 선택해서 실제처럼 제조/검사 시뮬레이션하는 프로그램”이다.

그러려면 먼저 장비를 데이터로 정의해야 한다.

```text
장비 템플릿
    ↓
모션 축 목록
    ↓
제품 recipe
    ↓
제품별 목표 위치와 검사 모드
```

이번 작업은 그 첫 데이터 모델이다.

## 바뀐 파일

- `src/EquipmentTwin.Core/Templates/EquipmentTemplate.cs`
- `src/EquipmentTwin.Core/Templates/MotionAxisTemplate.cs`
- `src/EquipmentTwin.Core/Templates/ProductRecipe.cs`
- `src/EquipmentTwin.Core/Templates/InspectionMode.cs`
- `templates/vision-inspection-cell.json`
- `tests/EquipmentTwin.Core.Tests/Program.cs`
- `goals/016-equipment-template.md`
- `README.md`
- `docs/architecture.md`
- `docs/core-validation.md`

## 검증 결과

- Release 빌드: 성공
- 경고: 0개
- 오류: 0개
- 로컬 테스트: 56개 통과
- CLI batch 실행: 9개 시나리오 통과
- GitHub Actions push 이벤트 CI: 성공
- GitHub Actions pull_request 이벤트 CI: 성공

## 막힌 점과 해결 방법

- 처음에 Release build와 Release test를 병렬로 돌려서 같은 DLL 파일을 동시에 잡는 lock 문제가 발생했다.
- 코드 문제는 아니었고, 순차 실행으로 다시 돌려 정상 통과를 확인했다.

## 보류한 판단

- 템플릿을 아직 ScenarioRunner 실행 계획으로 자동 변환하지 않는다.
- IO 정의는 아직 template에 넣지 않았다.
- Fault 조건은 아직 template에 넣지 않았다.
- Unity UI에서 template를 선택하는 기능은 아직 없다.

## 소프트웨어 아키텍처 설명

```text
templates/vision-inspection-cell.json
    ↓
EquipmentTemplate.FromJson()
    ↓
MotionAxisTemplate 검증
    ↓
ProductRecipe 검증
    ↓
CreateMotionAxes(clock)
```

`EquipmentTemplate`는 장비 전체 구성을 뜻한다.

`MotionAxisTemplate`는 장비가 가진 축을 뜻한다.

`ProductRecipe`는 제품별 목표 위치와 검사 모드를 뜻한다.

## 유지보수할 때 봐야 할 파일

- 장비 템플릿: `src/EquipmentTwin.Core/Templates/EquipmentTemplate.cs`
- 축 정의: `src/EquipmentTwin.Core/Templates/MotionAxisTemplate.cs`
- 제품 recipe: `src/EquipmentTwin.Core/Templates/ProductRecipe.cs`
- 검사 모드: `src/EquipmentTwin.Core/Templates/InspectionMode.cs`
- 샘플 JSON: `templates/vision-inspection-cell.json`
- 테스트: `tests/EquipmentTwin.Core.Tests/Program.cs`

## 사용자가 이해해야 할 개념

이번 작업은 장비를 UI로 만든 것이 아니라 장비를 데이터로 정의한 것이다.

이 단계가 있어야 나중에 Unity가 같은 데이터를 읽어서 장비를 그리고, CLI가 같은 데이터를 읽어서 테스트를 돌릴 수 있다.

## 다음 작업

1. PR #14 병합 여부를 결정한다.
2. 병합 후 `main`을 최신화한다.
3. 다음 후보로 Template Runner를 시작한다.

---

## Goal 017: Template Runner

## 오늘 추가로 한 일

- PR #14를 Draft 해제하고 squash merge했다.
- 로컬 `main`을 최신화했다.
- `goal/017-template-runner` 브랜치를 만들었다.
- Template Runner 최소 실행 계층을 추가했다.
- template/recipe를 읽어서 X/Z 축 ServoOn, Home, Move를 실행하게 했다.
- Template Runner 테스트 4개를 추가했다.
- Draft PR #15를 생성했다.
- GitHub Actions push/pull_request CI 성공을 확인했다.

## 왜 필요한가

Goal 016에서 장비와 제품 recipe를 JSON으로 정의했다.

하지만 정의만 있고 실행이 없으면 사용자가 “장비와 제품을 선택해서 실행한다”고 말하기 어렵다.

이번 작업은 아래 연결을 만든 것이다.

```text
EquipmentTemplate
    ↓
ProductRecipe
    ↓
TemplateRunner
    ↓
MotionAxis 실행
```

## 바뀐 파일

- `src/EquipmentTwin.Core/Templates/TemplateRunner.cs`
- `src/EquipmentTwin.Core/Templates/TemplateRunnerOptions.cs`
- `src/EquipmentTwin.Core/Templates/TemplateRunResult.cs`
- `src/EquipmentTwin.Core/Templates/TemplateMotionCommandLog.cs`
- `tests/EquipmentTwin.Core.Tests/Program.cs`
- `goals/017-template-runner.md`
- `README.md`
- `docs/architecture.md`
- `docs/core-validation.md`

## 검증 결과

- Release 빌드: 성공
- 경고: 0개
- 오류: 0개
- 로컬 테스트: 60개 통과
- CLI batch 실행: 9개 시나리오 통과
- GitHub Actions push 이벤트 CI: 성공
- GitHub Actions pull_request 이벤트 CI: 성공

## 막힌 점과 해결 방법

- build와 test를 다시 병렬로 돌려서 Release DLL lock 문제가 재발했다.
- 앞으로 Release 검증은 순차 실행해야 한다.
- `default-panel` recipe 명령 수를 처음에 12개로 잘못 계산했다. 실제 명령 수는 10개라 테스트 기대값을 수정했다.

## 보류한 판단

- Template Runner는 아직 모션 축만 실행한다.
- IO, 검사 결과, Fault Injection은 아직 연결하지 않았다.
- ScenarioRunner와 TemplateRunner는 아직 별도다.
- Unity UI에서 template/recipe를 선택하는 기능은 아직 없다.

## 소프트웨어 아키텍처 설명

```text
TemplateRunner.RunRecipe()
    ↓
FindProductRecipe()
    ↓
CreateMotionAxes()
    ↓
ServoOn / StartHome / Poll / StartMove / Poll
    ↓
TemplateRunResult
```

`TemplateRunResult`에는 recipe 정보, 최종 축 상태, 명령 로그가 남는다.

## 유지보수할 때 봐야 할 파일

- 실행 순서: `src/EquipmentTwin.Core/Templates/TemplateRunner.cs`
- 실행 옵션: `src/EquipmentTwin.Core/Templates/TemplateRunnerOptions.cs`
- 실행 결과: `src/EquipmentTwin.Core/Templates/TemplateRunResult.cs`
- 명령 로그: `src/EquipmentTwin.Core/Templates/TemplateMotionCommandLog.cs`
- 테스트: `tests/EquipmentTwin.Core.Tests/Program.cs`

## 사용자가 이해해야 할 개념

Template Runner는 “장비 선택”과 “실제 동작” 사이의 연결부다.

아직 전체 장비 공정을 실행하는 건 아니지만, 장비/제품 데이터가 실제 모션 실행으로 이어지는 첫 단계다.

## 다음 작업

1. PR #15 병합 여부를 결정한다.
2. 병합 후 `main`을 최신화한다.
3. 다음 후보로 Fault Model 또는 Inspection Result Model을 시작한다.

---

## Goal 018: Fault Model

## 오늘 추가로 한 일

- PR #15를 Draft 해제하고 squash merge했다.
- 로컬 `main`을 최신화했다.
- `goal/018-fault-model` 브랜치를 만들었다.
- Fault Model 최소 구조를 추가했다.
- Equipment Template에 `faultScenarios`를 추가했다.
- `x-axis-move-timeout`, `z-axis-servo-alarm` 샘플 fault를 추가했다.
- Template Runner가 선택한 fault를 실행 중 주입하게 했다.
- fault 관련 테스트 6개를 추가했다.
- Goal 018 변경을 `feat: add fault model` 커밋으로 정리했다.
- Draft PR #16을 만들었다: `https://github.com/sjsr-0401/equipment-twin-lab/pull/16`
- GitHub Actions `push`, `pull_request` CI가 성공했다.

## 왜 필요한가

사용자가 원하는 프로그램은 정상 공정만 돌리는 데모가 아니다.

장비/제품/공정/검사 조건과 함께 트러블 조건도 선택할 수 있어야 한다.

이번 작업은 트러블 조건을 데이터로 정의하는 첫 단계다.

```text
FaultScenario
    ↓
TemplateRunner
    ↓
MotionAxis.CheckTimeout() 또는 TriggerServoAlarm()
    ↓
TemplateRunResult.Success = false
```

## 바뀐 파일

- `src/EquipmentTwin.Core/Templates/FaultKind.cs`
- `src/EquipmentTwin.Core/Templates/FaultScenario.cs`
- `src/EquipmentTwin.Core/Templates/EquipmentTemplate.cs`
- `src/EquipmentTwin.Core/Templates/TemplateRunner.cs`
- `src/EquipmentTwin.Core/Templates/TemplateRunResult.cs`
- `templates/vision-inspection-cell.json`
- `tests/EquipmentTwin.Core.Tests/Program.cs`
- `goals/018-fault-model.md`
- `README.md`
- `docs/architecture.md`
- `docs/core-validation.md`

## 검증 결과

- Release 빌드: 성공
- 경고: 0개
- 오류: 0개
- 로컬 테스트: 66개 통과
- CLI batch 실행: 9개 시나리오 통과
- GitHub Actions: `push`, `pull_request` 성공

## 막힌 점과 해결 방법

- 이번 구현 자체에서 큰 막힘은 없었다.
- 이전 작업에서 확인한 DLL lock 문제를 피하기 위해 build/test/batch를 순차 실행했다.

## 보류한 판단

- IO fault는 아직 없다.
- Sensor stuck, door open, emergency stop을 template fault로 통합하지 않았다.
- 검사 PASS/FAIL 모델은 아직 없다.
- fault severity, operator action, recovery guide는 아직 없다.

## 소프트웨어 아키텍처 설명

```text
EquipmentTemplate
    ↓
FaultScenario
    ↓
TemplateRunner.RunRecipe(..., faultName)
    ↓
MotionAxis fault injection
    ↓
TemplateRunResult
```

현재 fault 종류는 두 가지다.

- `MotionTimeout`: move 중 지정 시간 초과
- `ServoAlarm`: move 중 서보 알람

## 유지보수할 때 봐야 할 파일

- fault 종류: `src/EquipmentTwin.Core/Templates/FaultKind.cs`
- fault 정의/검증: `src/EquipmentTwin.Core/Templates/FaultScenario.cs`
- template 연결: `src/EquipmentTwin.Core/Templates/EquipmentTemplate.cs`
- fault 실행: `src/EquipmentTwin.Core/Templates/TemplateRunner.cs`
- 샘플 fault: `templates/vision-inspection-cell.json`
- 테스트: `tests/EquipmentTwin.Core.Tests/Program.cs`

## 사용자가 이해해야 할 개념

Fault Model은 실제 물리 고장을 완벽히 재현하는 모델이 아니다.

지금은 장비 SW 관점에서 “이 트러블이 들어오면 어느 축이 어떤 알람 상태로 가는지”를 반복 검증하는 구조다.

## 다음 작업

1. PR #16 병합 여부를 결정한다.
2. 병합 후 `main`을 최신화한다.
3. 다음 후보로 Inspection Result Model을 시작한다.

---

## Goal 019: Inspection Result Model

## 오늘 추가로 한 일

- PR #16을 Draft 해제하고 squash merge했다.
- 로컬 `main`을 최신화했다.
- `goal/019-inspection-result` 브랜치를 만들었다.
- Inspection Result Model 최소 구조를 추가했다.
- `ProductRecipe`에 `inspectionResult`를 연결했다.
- `default-panel`은 PASS 검사 결과를 갖게 했다.
- `tall-part`는 `HEIGHT_OVER_LIMIT` Fail 검사 결과를 갖게 했다.
- Template Runner가 모션 성공 후 검사 결과를 생성하게 했다.
- 모션 fault가 있으면 검사 결과를 생성하지 않게 했다.
- 검사 결과 관련 테스트 2개를 추가하고 기존 Template Runner 테스트를 보강했다.
- Goal 019 변경을 `feat: add inspection result model` 커밋으로 정리했다.
- Draft PR #17을 만들었다: `https://github.com/sjsr-0401/equipment-twin-lab/pull/17`
- GitHub Actions `push`, `pull_request` CI가 성공했다.

## 왜 필요한가

제조 장비는 단순히 움직이는 것이 끝이 아니다.

무엇을 검사했고 결과가 PASS인지 FAIL인지 남겨야 한다.

이번 작업에서는 실제 카메라 없이도 검사 결과를 데이터로 표현하는 구조를 만들었다.

```text
ProductRecipe
    ↓
InspectionResultSpec
    ↓
TemplateRunner
    ↓
TemplateRunResult.InspectionResult
```

## 바뀐 파일

- `src/EquipmentTwin.Core/Templates/InspectionOutcome.cs`
- `src/EquipmentTwin.Core/Templates/InspectionResultSpec.cs`
- `src/EquipmentTwin.Core/Templates/InspectionResult.cs`
- `src/EquipmentTwin.Core/Templates/ProductRecipe.cs`
- `src/EquipmentTwin.Core/Templates/TemplateRunner.cs`
- `src/EquipmentTwin.Core/Templates/TemplateRunResult.cs`
- `templates/vision-inspection-cell.json`
- `tests/EquipmentTwin.Core.Tests/Program.cs`
- `goals/019-inspection-result-model.md`
- `README.md`
- `docs/architecture.md`
- `docs/core-validation.md`

## 검증 결과

- Release 빌드: 성공
- 경고: 0개
- 오류: 0개
- 로컬 테스트: 68개 통과
- CLI batch 실행: 9개 시나리오 통과
- GitHub Actions: `push`, `pull_request` 성공

## 막힌 점과 해결 방법

- 실제 카메라가 없으므로 이미지 기반 검사는 아직 할 수 없다.
- 그래서 recipe 안에 들어가는 `inspectionResult`를 가상 검사 결과로 정의했다.
- PowerShell에서 `Select-Object -Tail`이 동작하지 않아 `Get-Content -Tail`로 문서 끝부분을 확인했다.

## 보류한 판단

- 같은 recipe에서 PASS/FAIL inspection scenario를 선택하는 구조는 아직 없다.
- 실제 dataset camera adapter는 아직 없다.
- Unity 가상 카메라와 연결하지 않았다.
- tolerance, defect classification, retry 판정은 아직 없다.

## 소프트웨어 아키텍처 설명

```text
TemplateRunResult.Success
    = 장비 실행 성공 여부

TemplateRunResult.ProductPassed
    = 제품 검사 PASS/FAIL
```

장비가 정상으로 움직였어도 제품은 NG일 수 있다.

그래서 `tall-part`는 `TemplateRunResult.Success == true`이지만 `ProductPassed == false`가 될 수 있다.

## 유지보수할 때 봐야 할 파일

- 검사 결과 enum: `src/EquipmentTwin.Core/Templates/InspectionOutcome.cs`
- recipe 검사 결과 정의: `src/EquipmentTwin.Core/Templates/InspectionResultSpec.cs`
- 실행 후 검사 결과: `src/EquipmentTwin.Core/Templates/InspectionResult.cs`
- recipe validation: `src/EquipmentTwin.Core/Templates/ProductRecipe.cs`
- 검사 결과 생성: `src/EquipmentTwin.Core/Templates/TemplateRunner.cs`
- 샘플 데이터: `templates/vision-inspection-cell.json`
- 테스트: `tests/EquipmentTwin.Core.Tests/Program.cs`

## 사용자가 이해해야 할 개념

Inspection Result Model은 실제 비전 알고리즘이 아니다.

지금은 실제 카메라, 데이터셋 카메라, Unity 가상 카메라가 나중에 같은 형식으로 결과를 넘길 수 있게 만든 공통 결과 모델이다.

## 다음 작업

1. PR #17 병합 여부를 결정한다.
2. 병합 후 `main`을 최신화한다.
3. 다음 후보로 Template Runner CLI 또는 Inspection Scenario Selection을 선택한다.

---

## Goal 020: Template Runner CLI

## 오늘 추가로 한 일

- PR #17을 Draft 해제하고 squash merge했다.
- 로컬 `main`을 최신화했다.
- `goal/020-template-runner-cli` 브랜치를 만들었다.
- CLI에 `template run` 모드를 추가했다.
- template JSON과 recipe 이름을 명령어로 넘겨 실행할 수 있게 했다.
- `--fault <name>` 옵션으로 fault scenario를 주입할 수 있게 했다.
- CLI 출력에 `Execution`, `Product`, `Inspection`, `Motion axes`, `Command log`를 표시했다.
- GitHub Actions CI에 template CLI 검증 2개를 추가했다.
- Visual Studio launch profile에 template 실행 프로필을 추가했다.
- Goal 020 변경을 `feat: add template runner cli` 커밋으로 정리했다.
- Draft PR #18을 만들었다: `https://github.com/sjsr-0401/equipment-twin-lab/pull/18`
- GitHub Actions `push`, `pull_request` CI가 성공했다.

## 왜 필요한가

Core 안에 좋은 구조가 있어도 사람이 직접 실행해볼 수 없으면 데모가 약하다.

이번 작업은 Template Runner를 CLI로 노출해서 사용자가 장비 template, 제품 recipe, fault 조건을 직접 실행해볼 수 있게 만든 것이다.

```text
template JSON + recipe name
    ↓
EquipmentTwin.Cli
    ↓
TemplateRunner
    ↓
Execution / Product / Inspection 결과 출력
```

## 바뀐 파일

- `src/EquipmentTwin.Cli/Program.cs`
- `.github/workflows/ci.yml`
- `src/EquipmentTwin.Cli/Properties/launchSettings.json`
- `goals/020-template-runner-cli.md`
- `README.md`
- `docs/architecture.md`
- `docs/core-validation.md`
- `docs/visual-studio.md`

## 검증 결과

- Release 빌드: 성공
- 경고: 0개
- 오류: 0개
- 로컬 테스트: 68개 통과
- CLI batch 실행: 9개 시나리오 통과
- Template CLI `default-panel`: Execution PASS, Product PASS
- Template CLI `tall-part`: Execution PASS, Product FAIL
- Template CLI fault 출력: Execution FAIL, Product NOT_INSPECTED
- GitHub Actions: `push`, `pull_request` 성공

## 막힌 점과 해결 방법

- fault 주입 CLI는 의도적으로 exit code 1을 반환한다.
- 그래서 CI에는 fault 케이스를 넣지 않고, 정상 exit code 0인 `default-panel`, `tall-part`만 넣었다.
- fault 케이스는 로컬에서 출력만 확인했다.

## 보류한 판단

- template 실행 결과 Markdown 리포트는 아직 없다.
- template batch 실행은 아직 없다.
- fault를 “예상된 실패”로 CI에서 검증하는 방식은 아직 정하지 않았다.
- CLI 출력 포맷은 포트폴리오 MVP 수준이다.

## 소프트웨어 아키텍처 설명

```text
CliOptions.Parse()
    ↓
RunTemplate()
    ↓
EquipmentTemplate.FromJson()
    ↓
TemplateRunner.RunRecipe()
    ↓
PrintTemplateResult()
```

`Execution`은 장비 실행 결과이고, `Product`는 제품 검사 결과다.

`tall-part`는 장비 실행은 성공하지만 제품 검사는 Fail이다.

## 유지보수할 때 봐야 할 파일

- CLI 파싱/실행: `src/EquipmentTwin.Cli/Program.cs`
- CI 검증: `.github/workflows/ci.yml`
- Visual Studio 실행 프로필: `src/EquipmentTwin.Cli/Properties/launchSettings.json`
- template 샘플: `templates/vision-inspection-cell.json`

## 사용자가 이해해야 할 개념

이번 작업은 Unity UI를 만들기 전 단계의 조작 패널이다.

CLI에서 template/recipe/fault를 실행할 수 있으면, 나중에 Unity 버튼도 결국 같은 Core 기능을 호출하면 된다.

## 다음 작업

1. PR #18 병합 여부를 결정한다.
2. 병합 후 `main`을 최신화한다.
3. 다음 후보로 Inspection Scenario Selection 또는 Template Run Markdown Report를 선택한다.

---

## Goal 021: Template Run Markdown Report

## 오늘 추가로 한 일

- PR #18을 Draft 해제하고 squash merge했다.
- 로컬 `main`을 최신화했다.
- `goal/021-template-run-report` 브랜치를 만들었다.
- `template run` CLI에서 `--report <path>` 옵션을 허용했다.
- Template run 결과를 Markdown으로 저장하는 기능을 추가했다.
- report에 Summary, Inspection, Motion Axes, Command Log를 포함했다.
- CI의 template default-panel 실행에 report 생성을 포함했다.
- Visual Studio template default-panel launch profile에 report 생성을 포함했다.
- README, architecture, validation, Visual Studio 가이드를 갱신했다.
- Goal 021 변경을 `feat: add template run markdown report` 커밋으로 정리했다.
- Draft PR #19를 만들었다: `https://github.com/sjsr-0401/equipment-twin-lab/pull/19`
- GitHub Actions `push`, `pull_request` CI가 성공했다.

## 왜 필요한가

콘솔 출력은 실행 순간에는 좋지만, 포트폴리오나 작업 로그로 남기기 어렵다.

이번 작업은 template 실행 결과를 Markdown 파일로 남겨서 데모 자료로 사용할 수 있게 만든 것이다.

```text
template run ... --report artifacts\template-run-report.md
    ↓
TemplateRunner
    ↓
TemplateRunResult
    ↓
Markdown report
```

## 바뀐 파일

- `src/EquipmentTwin.Cli/Program.cs`
- `.github/workflows/ci.yml`
- `src/EquipmentTwin.Cli/Properties/launchSettings.json`
- `goals/021-template-run-markdown-report.md`
- `README.md`
- `docs/architecture.md`
- `docs/core-validation.md`
- `docs/visual-studio.md`

## 검증 결과

- Release 빌드: 성공
- 경고: 0개
- 오류: 0개
- 로컬 테스트: 68개 통과
- CLI batch 실행: 9개 시나리오 통과
- Template CLI `default-panel --report artifacts\template-run-report.md`: 성공
- Template CLI `tall-part`: 성공
- 생성된 Markdown report 내용 확인
- GitHub Actions: `push`, `pull_request` 성공

## 막힌 점과 해결 방법

- 검증 중 template CLI 두 개를 병렬 실행했을 때 DLL lock이 한 번 발생했다.
- 원인은 두 `dotnet run` 프로세스가 같은 Release build 출력물을 동시에 잡았기 때문이다.
- 이후 template CLI 검증을 순차 실행해서 통과시켰다.
- report 파일은 생성 시간이 매번 달라진다.
- 그래서 생성 결과물 자체는 커밋하지 않고, 생성 기능과 출력 내용만 확인했다.

## 보류한 판단

- template batch report는 아직 없다.
- 여러 recipe를 한 번에 실행하는 기능은 아직 없다.
- fault expected-failure report를 CI에서 검증하는 방식은 아직 정하지 않았다.

## 소프트웨어 아키텍처 설명

```text
RunTemplate()
    ↓
TemplateRunner.RunRecipe()
    ↓
BuildTemplateMarkdownReport()
    ↓
WriteTemplateMarkdownReport()
```

Core는 실행 결과를 만들고, CLI는 그 결과를 사람이 읽을 수 있는 문서로 바꾼다.

## 유지보수할 때 봐야 할 파일

- CLI 파싱/실행/report 생성: `src/EquipmentTwin.Cli/Program.cs`
- CI 검증: `.github/workflows/ci.yml`
- Visual Studio 실행 프로필: `src/EquipmentTwin.Cli/Properties/launchSettings.json`

## 사용자가 이해해야 할 개념

이 기능은 나중에 생산 이력, 검사 이력, 알람 이력 저장으로 확장할 수 있는 첫 단계다.

지금은 Markdown 파일이지만, 같은 데이터를 나중에는 DB나 MES 이벤트로 보낼 수 있다.

## 다음 작업

1. PR #19 병합 여부를 결정한다.
2. 병합 후 `main`을 최신화한다.
3. 다음 후보로 Inspection Scenario Selection 또는 Template Batch Report를 선택한다.

---

## Goal 022: Template Batch Report

## 오늘 추가로 한 일

- PR #19를 Draft 해제하고 squash merge했다.
- 로컬 `main`을 최신화했다.
- `goal/022-template-batch-report` 브랜치를 만들었다.
- CLI에 `template batch` 모드를 추가했다.
- template 안의 모든 product recipe를 한 번에 실행하게 했다.
- batch 콘솔 출력에 Execution/Product PASS/FAIL 요약을 추가했다.
- Template batch Markdown report를 생성했다.
- CI에 template batch report 생성을 추가했다.
- Visual Studio launch profile에 template batch report를 추가했다.
- README, architecture, validation, Visual Studio 문서를 갱신했다.
- Draft PR #20을 만들었다.
- GitHub Actions push/pull_request CI가 모두 성공했다.

## 왜 필요한가

단일 recipe 실행은 하나의 제품만 보여준다.

대표 프로젝트 데모에서는 여러 recipe를 한 번에 실행해서 어떤 제품은 PASS이고 어떤 제품은 FAIL인지 비교할 수 있어야 한다.

```text
template batch
    ↓
default-panel 실행
    ↓
tall-part 실행
    ↓
PASS/FAIL 비교 report
```

## 바뀐 파일

- `src/EquipmentTwin.Cli/Program.cs`
- `.github/workflows/ci.yml`
- `src/EquipmentTwin.Cli/Properties/launchSettings.json`
- `goals/022-template-batch-report.md`
- `README.md`
- `docs/architecture.md`
- `docs/core-validation.md`
- `docs/visual-studio.md`

## 검증 결과

- Release 빌드: 성공
- 경고: 0개
- 오류: 0개
- Template batch CLI: 성공
- 생성된 `artifacts\template-batch-report.md` 내용 확인
- GitHub Actions push CI: 성공
- GitHub Actions pull_request CI: 성공

## 막힌 점과 해결 방법

- 구현 중 C# top-level file에서 local function과 type 선언 순서 오류가 발생했다.
- `IsTemplateMode()`를 `CliOptions` 내부 static method로 옮겨 해결했다.
- 제품 FAIL은 batch 실패로 보지 않았다. 제품 FAIL은 품질 결과이고, 장비 실행 실패만 CLI 실패로 본다.

## 보류한 판단

- batch에서 fault scenario를 같이 실행하는 기능은 아직 없다.
- recipe별 fault/inspection scenario manifest는 아직 없다.
- batch report 결과물은 생성 시간이 달라져서 커밋하지 않는다.

## 소프트웨어 아키텍처 설명

```text
RunTemplateBatch()
    ↓
ProductRecipes 반복
    ↓
TemplateRunner.RunRecipe()
    ↓
TemplateBatchRun[]
    ↓
BuildTemplateBatchMarkdownReport()
```

## 유지보수할 때 봐야 할 파일

- batch 파싱/실행/report 생성: `src/EquipmentTwin.Cli/Program.cs`
- CI 검증: `.github/workflows/ci.yml`
- Visual Studio 실행 프로필: `src/EquipmentTwin.Cli/Properties/launchSettings.json`

## 사용자가 이해해야 할 개념

`Execution FAIL`과 `Product FAIL`은 다르다.

`tall-part`는 제품 검사 Fail이지만 장비 실행은 성공이므로 batch 자체는 성공으로 본다.

## 다음 작업

1. PR #20을 Ready로 전환하고 병합한다.
2. `main`을 최신화한다.
3. 다음 후보로 Inspection Scenario Selection 또는 Fault Expected-Failure Report를 선택한다.
